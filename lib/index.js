module.exports = {
    identity: require('./identity'),
    first: require('./first'),
    last: require('./last'),
    each: require('./each'),
    indexOf: require('./index_of'),
    filter: require('./filter')
};
